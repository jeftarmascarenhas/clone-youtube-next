import { Button } from '@material-ui/core';
import Layout from '../components/Layout';

export default function Home() {
  return (
    <div>
      <Layout title="Youtube">
        Clone youtube with next
        <Button color="primary">My Button</Button>
      </Layout>
    </div>
  );
}
